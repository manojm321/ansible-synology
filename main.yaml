---
- name: Setup synlogy NAS
  hosts: all
  become: yes
  roles:
    - python
    - docker
    - telegraf
    # tasks:
    # - name: Print all available facts
    #   ansible.builtin.debug:
    #     var: ansible_facts
